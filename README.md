Machine learning Internship Assisgnment1:
   List of Terminologies: 
         > Feature
         > Label
         > Prediction
         > Outlier
         > Test Data
         > Training Data
         > Model
         > Validation Data
         > Hyperparameter
         > Epoch
         > Loss Function
         > Learning Rate
         > Overfitting
         > Underfitting
         > Regularization
         > Cross-Validation
         > Feature Engineering
         > Dimensionality Reduction
         > Bias
         > Variance
